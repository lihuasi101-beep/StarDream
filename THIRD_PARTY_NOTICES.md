# Third-party notices

The original compatibility mode vendors the following runtime components:

- `js-dos` 8.4.1, including `js-dos.js`, `js-dos.css`, the DOSBox-X WebAssembly backend, and the emulator assets under `compat/runtime/`.
- License: GPL-2.0-only.
- Upstream package: https://www.npmjs.com/package/js-dos
- Source repository: https://github.com/caiiiycuk/js-dos
- Package metadata and build identifiers are retained in the downloaded `emulators.js` build.

The runtime is kept separate from the game data and is not modified by this project. When redistributing the compatibility build, preserve this notice and provide the corresponding js-dos source under the GPL-2.0 terms.
